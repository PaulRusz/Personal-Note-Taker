{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "saves personal notes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "if [ ! -d build ]; then mkdir build; fi && cp -r index.js package.json node_modules public routes views build/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {},
  "keywords": []
}
